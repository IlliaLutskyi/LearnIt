// import { DbLesson } from "@/types";

// export default async function updateSection(
//   req: Request,
//   params: Promise<{ id: string }>
// ) {
//   try {
//     // const data: { title: string; lessons: DbLesson[] } = await req.json();
//   } catch (err) {
//     return Response.json(
//       { message: "Could not update section" },
//       { status: 500 }
//     );
//   }
// }
